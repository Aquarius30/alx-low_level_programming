#include "main.h"
/**
 * more_numbers - prints 10x the numbers
 */
void more_numbers(void);
{
	int i;

	for (i = 0; i <= 10; i++)
	{
		int z;

		for (z >= 0; z <=14; z++)
		{
			_putchar(z);
		}
		_putchar(i);
	}
		_putchar('\n');
}
		
