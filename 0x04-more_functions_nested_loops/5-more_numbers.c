#include "main.h"
/**
 * more_numbers - prints 10x the numbers
 */
void more_numbers(void);
{
	int i;
	int z;

	for (i > 0; i <= 10; i++)
	{
		z = 0;
		while (z < 15)
		{
			_putchar(z + '0');
		}
		putchar('\n');
	}
}

		
